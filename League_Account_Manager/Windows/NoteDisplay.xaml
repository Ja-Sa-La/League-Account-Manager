<Window x:Class="League_Account_Manager.Windows.NoteDisplay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Height="460"
        Width="420" WindowStyle="None" WindowStartupLocation="CenterScreen" Background="{StaticResource AppBackgroundBrush}"
        MouseDown="Window_MouseDownDatadisplay"
        Deactivated="Window_Deactivated"
        Closing="Window_Closing"
        ResizeMode="NoResize">
    <Grid Background="{StaticResource AppBackgroundBrush}" Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource SurfacePanelStyle}" Padding="12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="Notes" Style="{StaticResource SectionTitleStyle}" />
            </Grid>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource SurfacePanelStyle}" Padding="8" Margin="0,8,0,0">
            <TextBox MouseDown="Window_MouseDownDatadisplay"
                     Name="Datathing" Margin="0"
                     TextWrapping="Wrap" Text=""
                     IsEnabled="True" IsReadOnly="False" VerticalScrollBarVisibility="Visible"
                     IsManipulationEnabled="True"
                     IsInactiveSelectionHighlightEnabled="True"
                     AcceptsReturn="True" Padding="10" Background="{StaticResource SurfaceRaisedBrush}"
                     BorderBrush="{StaticResource StrokeBrush}" Foreground="{StaticResource TextPrimaryBrush}" />
        </Border>
    </Grid>
</Window>