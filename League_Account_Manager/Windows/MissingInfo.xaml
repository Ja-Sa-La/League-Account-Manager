<Window x:Class="League_Account_Manager.Windows.MissingInfo"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Missing login information" Width="400" SizeToContent="Height" WindowStyle="None"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="{StaticResource AppBackgroundBrush}">
    <Border Margin="12" Style="{StaticResource SurfacePanelStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock Text="Login information required" Style="{StaticResource SectionTitleStyle}" />
            <TextBlock Grid.Row="1" Text="Enter the missing username and password to continue."
                       Foreground="{StaticResource TextSecondaryBrush}" Margin="0,5,0,12" />
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="8" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <ui:TextBox x:Name="username" PlaceholderText="Username" />
                <ui:PasswordBox Grid.Column="2" x:Name="password" PlaceholderText="Password" />
            </Grid>
            <ui:Button Grid.Row="3" Content="Continue" Margin="0,14,0,0"
                       HorizontalAlignment="Right" MinWidth="100" Click="Button_Click" />
        </Grid>
    </Border>
</Window>
