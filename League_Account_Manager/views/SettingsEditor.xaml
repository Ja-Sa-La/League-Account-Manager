<Page 
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml" x:Class="League_Account_Manager.views.SettingsEditor"
      mc:Ignorable="d"
      Title="SettingsEditor" d:DesignHeight="749" d:DesignWidth="1216">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="12">
                <TextBlock Text="Manage League config file protection and create portable settings backups."
                           TextWrapping="Wrap" Margin="0,0,0,12" />

                <Label Content="game.cfg" />
                <ui:TextBox x:Name="GameCfgPathBox" IsReadOnly="True" Margin="0,0,0,6" />
                <CheckBox x:Name="GameCfgReadOnly" Content="Set game.cfg to read-only" Margin="0,0,0,12" />

                <Label Content="PersistedSettings.json" />
                <ui:TextBox x:Name="PersistedSettingsPathBox" IsReadOnly="True" Margin="0,0,0,6" />
                <CheckBox x:Name="PersistedSettingsReadOnly" Content="Set PersistedSettings.json to read-only" Margin="0,0,0,12" />

                <WrapPanel Margin="0,0,0,16">
                    <ui:Button Content="Refresh file status" Click="OnRefreshClick" Width="150" Height="34" Margin="0,0,8,8" />
                    <ui:Button Content="Apply selected setting" Click="OnApplyReadOnlyClick" Width="170" Height="34" Margin="0,0,8,8" />
                </WrapPanel>

                <Separator Margin="0,0,0,12" />

                <TextBlock Text="Backup / Restore" FontWeight="SemiBold" Margin="0,0,0,8" />
                <WrapPanel>
                    <ui:Button Content="Export backup" Click="OnExportBackupClick" Width="130" Height="34" Margin="0,0,8,8" />
                    <ui:Button Content="Import backup" Click="OnImportBackupClick" Width="130" Height="34" Margin="0,0,8,8" />
                </WrapPanel>

                <TextBlock x:Name="StatusText" Text="" TextWrapping="Wrap" Margin="0,8,0,0" />
            </StackPanel>
        </ScrollViewer>

    </Grid>
</Page>