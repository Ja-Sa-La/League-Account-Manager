<Page 
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml" x:Class="League_Account_Manager.views.SettingsEditor"
      mc:Ignorable="d"
      Title="SettingsEditor" d:DesignHeight="749" d:DesignWidth="1216">

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Margin="0,0,0,16">
            <TextBlock Text="In-game settings" Style="{StaticResource PageTitleStyle}" />
            <TextBlock Text="Protect League configuration files and create portable backups."
                       Style="{StaticResource PageSubtitleStyle}" />
        </StackPanel>
        <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="14" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Border Style="{StaticResource SurfacePanelStyle}" VerticalAlignment="Top">
                    <StackPanel>
                        <TextBlock Text="File protection" Style="{StaticResource SectionTitleStyle}" Margin="0,0,0,14" />
                        <TextBlock Text="game.cfg" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,5" />
                        <ui:TextBox x:Name="GameCfgPathBox" IsReadOnly="True" Margin="0,0,0,6" />
                        <CheckBox x:Name="GameCfgReadOnly" Content="Set game.cfg to read-only" Margin="0,0,0,16" />
                        <TextBlock Text="PersistedSettings.json" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,5" />
                        <ui:TextBox x:Name="PersistedSettingsPathBox" IsReadOnly="True" Margin="0,0,0,6" />
                        <CheckBox x:Name="PersistedSettingsReadOnly" Content="Set PersistedSettings.json to read-only" Margin="0,0,0,16" />
                        <WrapPanel>
                            <ui:Button Content="Refresh status" Click="OnRefreshClick" Margin="0,0,8,8" />
                            <ui:Button Content="Apply selection" Click="OnApplyReadOnlyClick" Margin="0,0,8,8" />
                        </WrapPanel>
                    </StackPanel>
                </Border>
                <Border Grid.Column="2" Style="{StaticResource SurfacePanelStyle}" VerticalAlignment="Top">
                    <StackPanel>
                        <TextBlock Text="Backup and restore" Style="{StaticResource SectionTitleStyle}" Margin="0,0,0,8" />
                        <TextBlock Text="Export these files as a portable backup or restore one created earlier."
                                   TextWrapping="Wrap" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,14" />
                        <WrapPanel>
                            <ui:Button Content="Export backup" Click="OnExportBackupClick" Margin="0,0,8,8" />
                            <ui:Button Content="Import backup" Click="OnImportBackupClick" Margin="0,0,8,8" />
                        </WrapPanel>
                        <TextBlock x:Name="StatusText" Text="" TextWrapping="Wrap" Margin="0,12,0,0"
                                   Foreground="{StaticResource TextSecondaryBrush}" />
                    </StackPanel>
                </Border>
        </Grid>
    </Grid>
</Page>