<Page x:Class="League_Account_Manager.views.MiscTools"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d"
      d:DesignHeight="749" d:DesignWidth="1216"
      Title="MiscTools">

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Margin="0,0,0,16">
            <TextBlock Text="Miscellaneous tools" Style="{StaticResource PageTitleStyle}" />
            <TextBlock Text="Maintenance and diagnostic actions for Riot and League clients."
                       Style="{StaticResource PageSubtitleStyle}" />
        </StackPanel>
        <Border Grid.Row="1" Style="{StaticResource SurfacePanelStyle}" Margin="0,0,0,12">
            <WrapPanel>
                <ui:Button Click="OnNukeLogsClick" ContentControl.Content="Clear logs" Margin="0,0,8,8" />
                <ui:Button Click="OnUninstallLeagueClick" Content="Uninstall League" Margin="0,0,8,8" />
                <ui:Button Click="OnDisableAutolaunchClick" Content="Disable Riot autolaunch" Margin="0,0,8,8" />
                <ui:Button Click="OnGetRiotHwidClick" Content="Get Riot HWID" Margin="0,0,8,8" />
                <ui:Button Click="OnRestartLeagueUxClick" Content="Restart League UX" Margin="0,0,0,8" />
            </WrapPanel>
        </Border>
        <Border Grid.Row="2" Style="{StaticResource SurfacePanelStyle}" Padding="0">
            <TextBox Name="success" Text="Tool output will appear here." IsReadOnly="True"
                     Padding="14" TextWrapping="WrapWithOverflow" AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                     FontFamily="Cascadia Mono" FontSize="13" Background="Transparent"
                     BorderThickness="0" Foreground="{StaticResource TextPrimaryBrush}" />
        </Border>
    </Grid>
</Page>