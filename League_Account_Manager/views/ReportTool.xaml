<Page x:Class="League_Account_Manager.views.ReportTool"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d"
      d:DesignHeight="749" d:DesignWidth="1216"
      Title="ReportTool">

    <Grid Margin="18" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource SurfacePanelStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Vertical">
                    <TextBlock Text="Report tool" Style="{StaticResource PageTitleStyle}" />
                    <TextBlock Text="Review reportable players and submit selected reports."
                               Style="{StaticResource PageSubtitleStyle}" />
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                    <ui:Button Content="Get reportable players" Click="OnLoadReportablePlayersClick"
                               Margin="4,0,0,0" />
                    <ui:Button Content="Select all" Click="OnToggleSelectAllClick" Margin="4,0,0,0" />
                    <ui:Button Content="Report selected" Click="OnSendReportsClick" Margin="4,0,0,0" />
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource SurfacePanelStyle}" Padding="10" Margin="0,12,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock x:Name="Status" Text="Ready to load accounts" FontSize="14"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           Margin="4,0,0,6" />
                <DataGrid Grid.Row="1" Name="Reportable" AutoGenerateColumns="False"
                          SelectionMode="Single" CanUserResizeColumns="False"
                          CanUserReorderColumns="False" CanUserResizeRows="False"
                          CanUserDeleteRows="False" CanUserAddRows="False" HorizontalContentAlignment="Stretch"
                          VerticalContentAlignment="Center"
                          ScrollViewer.VerticalScrollBarVisibility="Auto"
                          ScrollViewer.HorizontalScrollBarVisibility="Auto">
                    <DataGrid.Columns>
                        <DataGridCheckBoxColumn Binding="{Binding report}" Header="Select" Width="60" />
                        <DataGridTextColumn Binding="{Binding riotID}" Header="Username" Width="*" />
                        <DataGridTextColumn Binding="{Binding reported}" Header="Reported" Width="100" />
                        <DataGridTextColumn Binding="{Binding summonerId}" Header="SummonerID" Width="150" />
                        <DataGridTextColumn Binding="{Binding gameId}" Header="GameID" Width="150" />
                        <DataGridTextColumn Binding="{Binding puuId}" Header="puuID" Width="*" />
                    </DataGrid.Columns>
                    <DataGrid.CellStyle>
                        <Style TargetType="DataGridCell" BasedOn="{StaticResource AppDataGridCellStyle}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type DataGridCell}">
                                        <Border Padding="{TemplateBinding Padding}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                                            <ContentPresenter
                                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="{StaticResource SurfaceHoverBrush}" />
                                    <Setter Property="BorderBrush" Value="{StaticResource SurfaceHoverBrush}" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.CellStyle>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</Page>